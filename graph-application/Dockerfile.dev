FROM emscripten/emsdk:3.1.64 AS dev
WORKDIR /app
COPY wasm-engine ./wasm-engine
COPY frontend ./frontend
WORKDIR /app/frontend
RUN npm ci --no-audit --no-fund --prefer-offline
EXPOSE 5173
CMD ["sh", "-lc", "cd /app && make -C wasm-engine wasm && cd frontend && npm run dev"]
